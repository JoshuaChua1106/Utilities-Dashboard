# Environment Configuration
ENVIRONMENT=local
AWS_MODE=false

# Database Configuration
DATABASE_TYPE=sqlite
DATABASE_PATH=./data/invoices.db

# AWS Database Configuration (when AWS_MODE=true)
# RDS_ENDPOINT=your-rds-endpoint.region.rds.amazonaws.com
# RDS_DATABASE=utilities_tracker
# RDS_USERNAME=postgres
# RDS_PASSWORD_SECRET_NAME=rds-password

# Storage Configuration
STORAGE_TYPE=filesystem
STORAGE_BASE_PATH=./data/invoices/

# AWS Storage Configuration (when AWS_MODE=true)
# S3_BUCKET_NAME=utilities-tracker-invoices
# S3_REGION=us-east-1

# OCR Configuration
OCR_TYPE=local
OCR_ENGINES=pdfplumber,pytesseract

# AWS OCR Configuration (when AWS_MODE=true)
# TEXTRACT_REGION=us-east-1

# API Configuration
API_HOST=localhost
API_PORT=5000
WEB_HOST=localhost
WEB_PORT=3000

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=./logs/utilities-tracker.log

# AWS Logging Configuration (when AWS_MODE=true)
# CLOUDWATCH_LOG_GROUP=/aws/utilities-tracker
# CLOUDWATCH_LOG_STREAM=main

# Email Configuration
EMAIL_PROVIDER=gmail
MAX_EMAIL_RESULTS=100
EMAIL_SEARCH_DAYS=90

# Processing Configuration
MAX_RETRY_ATTEMPTS=3
RETRY_DELAY_SECONDS=5
BATCH_SIZE=10

# Security Configuration (Local)
SECRET_KEY=your-secret-key-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key

# AWS Security Configuration (when AWS_MODE=true)
# AWS_REGION=us-east-1
# SECRETS_MANAGER_PREFIX=utilities-tracker/
# KMS_KEY_ID=alias/utilities-tracker